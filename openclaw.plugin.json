{
  "id": "open-lesson",
  "name": "openLesson",
  "description": "openLesson Agentic API v2 — Full integration with 28 endpoints: learning plans (CRUD, AI adaptation, YouTube import), tutoring sessions (multimodal analysis, pause/resume/restart), teaching assistant, analytics, and cryptographic proofs",
  "version": "2.0.0",
  "author": "openLesson",
  "homepage": "https://www.openlesson.academy",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiKey": {
        "type": "string",
        "description": "openLesson API key (sk_...) for authentication. Generate from your dashboard."
      },
      "baseUrl": {
        "type": "string",
        "default": "https://www.openlesson.academy",
        "description": "API base URL (default: https://www.openlesson.academy)"
      }
    },
    "required": ["apiKey"]
  },
  "uiHints": {
    "apiKey": {
      "label": "API Key",
      "sensitive": true,
      "placeholder": "sk_..."
    },
    "baseUrl": {
      "label": "API Base URL",
      "placeholder": "https://www.openlesson.academy"
    }
  }
}
